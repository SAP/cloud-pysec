sudo: false
language: python
matrix:
  fast_finish: true
  include:
    - python: 3.7
      env: TOXENV=py37
    - python: 3.8
      env: TOXENV=py38
before_install:
  - python -m pip install --upgrade virtualenv
install:
  - pip install -U tox

script: tox
